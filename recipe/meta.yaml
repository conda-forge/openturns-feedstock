{% set version = "1.26.1" %}

package:
  name: openturns
  version: {{ version }}

source:
  fn: openturns-{{ version }}.tar.gz
  url: https://github.com/openturns/openturns/archive/v{{ version }}.tar.gz
  sha256: 41ddda08a44ea9980e725987669d49448280acb2c8e06d9d279a7321ec9452c0

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('openturns', max_pin='x.x') }}
  python_version_independent: true  # [is_abi3]
  skip: true  # [is_abi3 and not is_python_min]

requirements:
  build:
    - cmake
    - ninja
    - swig
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}
    - m2-diffutils  # [win]
    - diffutils  # [linux]
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
  host:
    - python-abi3  # [is_abi3]
    - python
    - libboost-headers
    - matplotlib-base  # [python_impl == 'cpython']
    - mpc
    - libblas
    - liblapack
    - libxml2-devel
    - tbb-devel
    - cminpack
    - ceres-solver * *lgpl*
    - glog
    - nlopt
    - dlib-cpp
    - ipopt
    - coin-or-bonmin  # [not win]
    - spectralib
    - hdf5
    - highs
    - pagmo-devel
    - libboost-devel
    - primesieve
    - libcuba
    - nanoflann
    - zlib
  run:
    - python
    - dill
    - psutil
    - tbb-devel
    - highs
    - ceres-solver * *lgpl*

test:
  requires:
    - abi3audit  # [is_abi3]
  imports:
    - openturns
  commands:
    - abi3audit $SP_DIR/openturns/_common.so -s -v --assume-minimum-abi3 3.10  # [unix and is_abi3]
    - abi3audit %SP_DIR%\\openturns\\_common.pyd -s -v --assume-minimum-abi3 3.10  # [win and is_abi3]
    - if not exist %PREFIX%\\Library\\bin\\openturns.conf exit 1  # [win]

about:
  home: http://openturns.org/
  license: LGPL-3.0-or-later
  license_family: LGPL
  license_file: COPYING.LESSER
  summary: Uncertainty treatment library

extra:
  recipe-maintainers:
    - jschueller
