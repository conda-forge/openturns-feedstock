{% set version = "1.14rc1" %}

package:
  name: openturns
  version: {{ version }}

source:
  fn: openturns-{{ version }}.tar.gz
  url: https://github.com/openturns/openturns/archive/v{{ version }}.tar.gz
  sha256: 4c0c24dc1b8307e344313475ced5a9a5971a7d2ba11d39e2c2876ac27cf1660e

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('openturns', max_pin='x.x') }}

requirements:
  build:
    - cmake  # [not win]
    - swig  # [not win]
    - {{ compiler('c') }}  # [not win]
    - {{ compiler('cxx') }}  # [not win]
  host:
    - boost-cpp  # [not win]
    - python
    - matplotlib
    - liblapack  # [not win]
    - libxml2  # [not win]
    - muparser  # [not win]
    - tbb-devel  # [not win]
    - nlopt  # [not win]
    - hmat-oss  # [not win]
    - cminpack  # [not win]
    - ceres-solver  # [not win]
    - dlib  # [not win]
    - coinbonmin  # [not win]
  run:
    - python
    - psutil
    - hmat-oss  # [not win]
    - cminpack  # [not win]
    - dlib  # [not win]

test:
  imports:
    - openturns

about:
  home: http://openturns.org/
  license: LGPL3
  license_file: COPYING.LESSER
  summary: Uncertainty treatment library

extra:
  recipe-maintainers:
    - jschueller
